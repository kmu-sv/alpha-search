CREATE TABLE CAFES_REVIEWS (
	id INT PRIMARY KEY AUTO_INCREMENT,
	cafe_id INT NOT NULL,
	reviews LONGTEXT,
    FOREIGN KEY (cafe_id) REFERENCES CAFES_YELP (id)
    ON DELETE CASCADE
    ON UPDATE RESTRICT
) ENGINE = InnoDB;